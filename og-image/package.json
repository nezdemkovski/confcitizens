{
  "name": "og-image",
  "version": "1.0.0",
  "description": "Generate an open graph image for twitter/facebook/etc",
  "license": "MIT",
  "main": "src/card.ts",
  "dependencies": {
    "chrome-aws-lambda": "1.14.0",
    "marked": "^0.6.1",
    "puppeteer-core": "1.14.0"
  },
  "devDependencies": {
    "@types/marked": "^0.6.3",
    "@types/puppeteer-core": "^1.9.0",
    "typescript": "^3.3.4000"
  },
  "scripts": {
    "start": "node dist/card.js",
    "build": "tsc",
    "now-build": "tsc",
    "watch": "tsc --watch",
    "dev": "nodemon -e js,ts,tsx -x 'ts-node src/card.ts' -w src"
  }
}
